package sailpointproject.Rule;

import sailpoint.api.SailPointContext;
import sailpoint.object.ManagedAttribute;


/*
 * Author 	: Aakash Pandita
 * Arguments 	: identity, context
 * Output	: we need to create a make entitlements requestable and set owners for them. we can also set level of approval as per environment
 */
public class MakeEntitlementRequestable 
{
	
	
	public void execute(SailPointContext context, String groupApplication, ManagedAttribute accountGroup)
	{
		
	}
}
